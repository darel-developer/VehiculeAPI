# Spring Data JPA configuration
spring.datasource.url=${DATABASE_URL:jdbc:mariadb://vehicle-williams25prince-0f50.j.aivencloud.com:18290/defaultdb?useSSL=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:avnadmin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:AVNS_x4rGbL4PeJ3KqbxuTgg}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect

# JWT configuration
jwt.secret=${JWT_SECRET:your_jwt_secret}
jwt.expiration=${JWT_EXPIRATION:3600000}

# Server configuration
server.port=8080

# Logging configuration
logging.level.org.springframework=INFO
logging.level.com.propelize.vehicleapi=DEBUG